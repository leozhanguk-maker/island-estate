# 每晚巡检报告

最近 7 轮完整记录，更早的压缩为摘要。

## 2026-09-23 夜间（本轮）

**起点**：main 落后于一条已完成但未合并的 PR 链（#1→#2→#3→#4，均来自同一会话 `session_01YVDTLzEwX4DFsT7uaZFSHy`，创建于当天 13:49～18:21）。该链已经搭好 `npm run patrol` / `audit:scene` / `shots` / `test:visual` / `test:invariants` / `test:baselines` / `test:regression` / `check` 全套工具，并用它们修复了 9 项程序缺陷、9 项视觉缺陷，`docs/ISSUES.md` 已建好。本轮没有从零重建这些工具，而是先核实、再顺着已有工作继续。

### 合并到 main
- **#1 云端测试环境、boat_test 修复与别墅/泳池错位修复**——修复 BOXG 事故（`box()` 共享几何体被原地平移，导致泳池冒出地面、别墅首层墙体悬空）；固定 `playwright==1.56.0` 匹配容器内置 chromium-1194。
- **#2 自动找错系统**——巡逻机器人、场景体检、截图巡检、视觉回归、基线不变量、合并门禁，新增 `docs/ISSUES.md` 统一问题清单。
- **#3 修复程序缺陷 9 项**——P-001 礁石吞人、P-002 潜水嵌底、P-003/P-004 可行走面缺失、P-005 碰撞缺失、P-006 墙线段重复、P-008 直升机高空穿楼、P-009 闸门落水、P-011 测试工具遗留问题。

合并前在 fix-program 分支尖端跑过完整 `npm run check`（构建 + 地形 + 基线不变量 + 5 项浏览器基线断言 + 回归 + 视觉回归 + 快速巡逻 + 严格体检），全绿；`git diff` 确认合并后的 main 与验证过的内容逐字节一致。合并到 main 后又单独跑了一次 `test:terrain` + `patrol`（71 个计划，正式种子）+ `scene_audit --strict`：**新问题 0 个**。

### 待你审阅
- **PR #4：修复视觉缺陷 9 项**（汀步、厨房吊柜、住宅楼前庭、栈桥桩柱水下、鸡舍窗、别墅南门花池、共面闪烁、车越坡顶、塔顶控制台），附同机位前后对比图：https://github.com/leozhanguk-maker/island-estate/pull/4
  按仓库合并策略，视觉改动等你审阅后再合并，本轮未自行合并，只把目标分支从 #3 的分支改回了 `main`（内容未变）。

### 待你决定（非缺陷，设计选择）
- **P-007**：离开游艇舵位后，舵角/油门是否应归零？目前保留不归零，船会原地打转或继续开。见 `docs/ISSUES.md`。
- **V-009**：直升机尾梁/垂尾比例是否需要调整？需要你给出期望效果。
- **V-010**：游艇后甲板（准确说是紧邻船尾的登岸浮台，`src/06_struct_b.js` 的 `box(..., M.metalMid, ...)`）material 偏灰是否要改？本轮截图复核确认了这一观感（浮台 0.55 m 高的立面几乎全是灰色金属材质，只有顶面一圈 0.12 m 厚的柚木色压顶），但因为没有你对"应该长什么样"的明确要求，没有动手改。

### 本轮未发现新问题
- 独立跑了一次真实 `npm run patrol`（正式种子 20260923，71 个计划，78 秒，覆盖漫游/游泳/开车/开拖拉机/开船/自动巡航/直升机）与 `npm run audit:scene --strict`：均为 0 个新问题。
- 用 `tests/shot.py` 对多个机位（鸟瞰、别墅、港口、机库停机坪、住宅楼、农业区）截图人工复核，与 `docs/ISSUES.md` 现有记录一致，没有发现新的视觉缺陷。
- 巡逻中 `car-road3` 计划报了 18 个问题，核实为已登记的 V-007（车身越过坡顶陷入地面）重复触发，非新问题；该项已在待审阅的 PR #4 中修复。

### 本轮放弃/未处理
- 无。没有尝试后回退的改动。

## 更早摘要
无（这是第一轮报告）。
