# 问题清单

所有问题统一记在这里，由巡逻机器人（`npm run patrol`）、场景体检（`npm run audit:scene`）、截图巡检（`npm run shots`）和人工审阅产生。

## 约定
- **分类**：程序缺陷（逻辑、物理、碰撞、数据错误）/ 视觉缺陷（画面上能看出来的错误）/ 审美建议（不算错，但可以更好；只记录，不自动改）。
- **严重度**：高（崩溃、卡死、主线功能不可用）、中（明显影响体验，但有绕行办法）、低（小瑕疵）。
- **状态**：待修、修复中、已修复（注明提交）、暂缓（注明原因）、无需处理（设计如此或误报）。
- **编号**：程序缺陷 `P-xxx`，视觉缺陷 `V-xxx`，审美建议 `A-xxx`。编号只增不复用。
- **签名**：工具输出的问题带签名（如 `穿墙|315,-115`）。已登记的问题签名写进 `tests/known_issues.json` 并注明编号，`--strict` 门禁只会拦截新出现的问题。修复后要从 `known_issues.json` 删除对应签名，工具会提示"已登记但本轮未再出现"的条目。
- 每条问题都要有：位置坐标、复现步骤（或工具签名与复现命令）、来源，能判断的附上原因。

## 程序缺陷

### P-001 陡坡上的礁石会把人"吞"进去　【中｜已修复】
- **位置**：(314.6, -113.3) 东海岸崖壁礁石 r=1.93；(186.7, 185.7) 南岸礁石 r=1.34。
- **复现**：`python3 tests/patrol.py --seed=7 --repro=walk-12`；水中那处用 `--repro=swim-05`。
- **现象**：人物中心嵌进礁石的圆柱碰撞体，四周迈不出去，1.2 秒后卡住保护把人传送回起点。
- **原因**：`src/11_fp.js` 的移动循环中，`pushOut` 算出的推出点要再过一遍 `canStep`。礁石长在陡坡上，推出点的坡度超过可攀爬上限，这次推出就被拒绝，人留在碰撞体里。
- **修复**：推出点不可达时退回本步起点，人不会再迈进实心体（`src/11_fp.js` 移动循环）。回归：`tests/regression.py` 严格重放上述两个计划。

### P-002 潜到最深处朝岸游，会嵌进海底斜坡　【低｜已修复】
- **位置**：(-272.4, -149.5)、(184.6, 187.4)、(141, -199) 等水下斜坡，嵌入 0.3～1.2 m。
- **复现**：`python3 tests/patrol.py --seed=20260923 --repro=swim-06`。
- **原因**：游泳时 `canStep` 只检查新位置的水底是否低于水面 +1.1，不检查水底是否高于当前脚底。下潜目标深度虽然每帧重算，但上浮速度（1.6 m/s）赶不上斜坡抬升。
- **修复**：游泳状态下脚底不低于当前位置的水底（`src/11_fp.js` 竖向状态机）。回归：`tests/regression.py`。

### P-003 泳池石材压顶没登记可行走面　【低｜已修复】
- **位置**：泳池四边压顶 (180, -61.1)、(180, -56.1)、(172.45, -58.6)、(187.55, -58.6)，顶面 11.58。
- **现象**：压顶高出木平台 0.18 m，人走上去脚底仍在木平台高度，脚陷进石材里。
- **来源**：场景体检"可行走面未登记"，截图核实（问题特写 27～30）。
- **修复**：四段压顶登记为可行走面（顶面 11.58）。回归：`scene_audit --strict`。

### P-004 光伏区底座、别墅花盆等没登记可行走面或碰撞　【低｜已修复】
- **位置**：牛棚地坪 (-201, -58) 27×13 m 高 0.3；猪舍/鸡舍地坪 (-129, 46.2)、(-97, 46.2)；食槽 (-124, 56)；光伏区 (250.5, -56)；别墅内方凳 4 处；湖岸汀步 (171.42, -67.75)。
- **更正**：牛棚、猪舍、鸡舍、食槽 4 条是检测器误报。漫游物理在 `setupFP` 里把牛棚、圈舍整体登记为实心体，人根本进不去，而检测器原来只读了 `COLL`。检测器已改用完整的碰撞列表。
- **修复**：储能墙底座登记为可行走面；别墅内的方凳状花盆由 `planter()` 统一登记碰撞。湖岸汀步随 V-001 一并处理。

### P-005 一批构件没有碰撞，人可以直接穿过　【低｜已修复】
- **范围**：场景体检"构件无碰撞"共 77 条。主要是：
  - 葡萄园藤架行 (-62～-47, -80～-31)，高 1.25 m，占大多数；
  - 别墅南门两侧花池 (177, -36.5)、(187.5, -36.5)；
  - 住宅楼入口花坛与长椅 (±10, -135)、(±5.5, -134.5)；
  - 牛棚食槽 (-201, -62.8)；
  - 光伏区设备柜 (247.5, -42/-44)，高 1.9 m；
  - 东峰塔顶控制台 (219.7, -147.6)。
- **更正**：葡萄园藤架行（67 条）是检测器误报，每行四周其实已有墙线段；住宅楼底座也是误报，整栋是实心体。两处检测器都已修正。
- **修复**：`planter()` 传入世界偏移时自动登记实心碰撞（别墅花池、花盆），住宅楼前庭绿篱与长椅、光伏设备柜补碰撞；原先手写的两处重复花池碰撞已删除。
- **例外**：东峰塔顶控制台加碰撞会堵死楼梯出口，改记为 V-013，随视觉修复一起处理。

### P-006 墙线段重复登记　【低｜已修复】
- **位置**：东峰瞭望塔每层楼梯井 (218, -146.5)，水闸塔 (46.5, 119.4～121.6) 等，共 43 条。
- **影响**：不影响手感，但碰撞检测多做了一倍的工作，代码也有冗余。
- **原因与修复**：`towerStairs()` 的"梯段隔离"线段与 `stairs()` 在梯段内侧边登记的线段完全相同，已删除，共去掉 31 条。
- **剩余**：26 条"碰撞体重叠"是野外树干与礁石的圆柱碰撞体互相套叠，无害，标为无需处理。

### P-007 按住转向键离开游艇舵位，船会原地打转　【低｜已修复】
- **现象**：舵角 `BOAT.rud` 和油门 `BOAT.thr` 只在驾驶时更新，离开舵位时的值会一直保留。复现：开阔海面全速左满舵后按 E 离开，20 秒后船仍以 9.5 m/s 航行并转了 3.4 rad。
- **原因**：`src/13_drive.js` 的 `exitCar()` 对游艇只清除 `DRIVE.active`，没有复位油门和舵角。
- **修复**：按你的决定，离开舵位时油门、舵角都归零，船靠水阻自然减速停下（约 10 秒）。自动巡航不受影响。回归：`tests/regression.py`（P-007 用例）。

### P-008 直升机只在离地 4 m 内检测碰撞　【中｜已修复】（原 HANDOVER 待办）
- 手动飞行可能穿过住宅楼上部。
- **修复**：建筑构建后把有体量的构件包围盒交给直升机（`setHeliObstacles`），任何高度下机舱碰到都会被挡住；贴地时仍沿用原碰撞登记（含树木、礁石）。自动往返航线的用时与落点不变。回归：`tests/regression.py`，直升机在 25 m 高度朝住宅楼平飞。

### P-009 开闸时走上正在下沉的闸门，会随闸门落水　【低｜已修复】（原 HANDOVER 待办）
- 只在点击开闸那一刻转移玩家（`src/10_main.js` 主循环）。
- **修复**：闸门逻辑抽成 `updateGate()`，主循环与测试共用。门顶可行走面存在期间（`open < 0.02`）持续转移门顶上的人。回归：`phys_test` 的 `g_openWalk` 改走开闸全流程，并在 `test:baselines` 中断言。

### P-010 别墅泳池机位渲染热点　【低｜暂缓】
- 高档画质下该机位 350 万三角面、1481 次绘制，渲染耗时是各机位中位数的约 4 倍（巡逻"掉帧"）。
- 同类热点还有登岸浮台、游艇后甲板等机位（倍数随每次测量波动）。
- 2026-09-24 23:40（新加坡时间）加入三水域生态后，沙滩机位也出现一次（24.4 ms，中位数 3.0 倍，1092 次绘制）；该机位能看到潟湖生态，生态在潟湖水下新增约 26 次绘制。同样暂缓。
- 按 CLAUDE.md，性能优化要等你确认成品后再做。巡逻的 `--strict` 门禁不因"掉帧"判失败（软件渲染耗时不稳定），只报告。

### P-011 测试工具的遗留问题　【低｜已修复】
- `tests/fp_shot.py` 的 `deck` 预设传送时没带 onBoat 标志，截图时人在水里。
- `tests/phys_test.py` 的 `g_openWalk` 直接设置 `GATE.open`，绕过了主循环的转移逻辑（原 HANDOVER 待办）。
- **修复**：`deck` 预设改为传送到船上；`g_openWalk` 改走开闸全流程（见 P-009）。

### P-012 在 Claude 网页预览中打开，岛上漫游报错并卡死　【高｜已修复】
- **现象**：预览页面禁止创建后台线程，控制台先出现“后台生成不可用，回退主线程”，进入漫游并传送到有草地的地方（如沙滩）后报 `Cannot read properties of undefined (reading 'NaN')`，主循环随之停止，之后点传送没有任何反应。下载到本地运行时后台线程可用，不报错。
- **复现**：`#q=high,debug,noworker` 强制主线程生成，进入漫游传送到 (0, 20)。只在高、中档出现（低档不生成近景草叶）。
- **原因**：主线程回退路径 `generateIsland` 的 `onMain()`（`src/00_gen.js`）把地表贴图原样以 canvas 元素返回，而后台线程返回的是 `{data, w, h}` 像素数据。近景草叶取色 `colAt()`（`src/12_grass.js`）按像素数据读取 `gd.w`、`gd.data`，在 canvas 上都是 undefined，下标成了 NaN 并抛错；小地图底图同样读不到像素，回退时是空白的。
- **修复**：`onMain()` 与后台线程一样，把地表贴图和遮罩转成 `{data, w, h}` 再返回。回归：`tests/regression.py`（P-012 用例：无后台线程、高档画质、传送到沙滩后按 W 前进，要求不报错且位置改变）。

### P-013 湖岸泥色的判断符号反了，全岛地表底色都被涂成泥色　【高｜已修复】
- **现象**：港口沙滩是灰褐色，不是设计中的“碧海银沙”（审美建议 A-001 记录的就是这个）。草地、林地的地表底色也都是同一种褐色 (90, 84, 62)，只是被农田、道路、植被遮住了一部分。漫游时草地上没有近景草叶。
- **复现**：读取地表贴图像素，港口沙滩 (0, 25)、草地 (120, 40) 都是 (90, 84, 62)；草地周围的近景草叶为 0 丛。
- **原因**：`lakeSD()`（`src/03_terrain.js`）湖内为正、湖外为负，其他调用处都按这个约定写的。只有两处写反了：
  - `src/04_ground.js` 的湖岸泥色按“湖外为正”判断 `lk < 0.3`，湖外恒成立且权重为 1，全岛湖外的底色都被泥色 [92, 86, 64] 覆盖；
  - `src/04b_detail.js` 的材质权重图同样判断反了，湖外全岛都被设成“草 0、沙 0.5、土 0.5”，近景草叶的生长条件（草权重 ≥ 0.7）在草地上从来不成立。
  - 两处都是最初上传的版本就有。
- **修复**：两处改为按“离湖岸外侧的距离 = -lakeSD”判断。修复后沙滩为银白细沙，草地恢复绿色，湖底仍是深色泥，漫游时草地上有近景草叶。回归：`tests/invariants.py` 检查沙滩、草地、湖底的地表色和草地上的草叶数量（修复前 5 条失败）。

### P-014 全岛植被共用一条随机数序列，局部地形改动会让全岛树木整体挪位　【低｜暂缓，待你决定】
- **现象**：2026-09-25 01:20（新加坡时间）为栈道削平岩石（`carveBoardwalk`）后，只改了东岸栈道两侧 4 m 的地形，别墅、宿舍楼、鸟瞰等 8 个视觉回归机位的远山树木都换了位置（差异像素 1～10%）。圆柱碰撞体多了 54 个（树与大块岩石）。
- **原因**：`src/08_vegetation.js` 的抖动网格撒点从头到尾共用一个 `mulberry32(4242)`，每个格点抽几次随机数取决于这个格点是否被接受（高度、法线、道路、排除区）。局部地形一变，被接受的格点变了，之后所有格点抽到的随机数都错位；后面给每棵树选树种、大小时也共用序列，同样整体错位。
- **本次处理**：接受这次重排，数量基线与视觉基线各单独提交并附对比图。
- **建议修复**：改为按格点坐标取随机数种子（每个格点独立），以后任何局部改动只影响附近的树。代价是全岛植被会一次性重新排布，需要你确认后再做。

### P-015 湖里的溪蟹会爬到岸边水线以上的卵石上　【低｜已修复】
- **现象**：2026-09-25 04:40（新加坡时间）`npm run test:ecology` 偶发失败：“lake 有 1 个生物钻进水底或跑出水面，高度 10.44，地面 10.44 @ (183.1, -68.2)”（湖面 10.3）。
- **复现**：`#still` 模式下页面先按真实帧间隔跑 3 帧，随机游走状态每次不同，所以时有时无。确定性复现：检查溪蟹可选的窝，其中有岸边水线以上的卵石。
- **原因**：`src/17b_eco_lake.js` 的溪蟹在“大卵石”之间横行，大卵石取自整个卵石撒点，撒点范围包含岸边水线以上 0.3 m 的一圈，溪蟹选中这些卵石就会爬出水面。
- **修复**：溪蟹只把水深 ≥ 0.2 m 的大卵石当作窝。回归：`tests/ecology_test.py` 逐个检查溪蟹可选的窝都在水下（确定性，不依赖随机游走）。

### P-016 三个水域的生态共用一条随机数序列，改湖会让潟湖、外海的布置整体错位　【低｜已修复】
- **现象**：2026-09-25 05:50（新加坡时间）把淡水湖改成水族馆式后，潟湖礁盘、海草床在原机位上都看不见了（换到了别处）。
- **原因**：`ECO_R` 是全局唯一的随机数序列，湖、潟湖、外海依次建造。湖里抽的随机数个数一变，后面两个水域的所有随机位置都错位（与 P-014 植被同类）。
- **修复**：`ecoSeed(n)` 在每个水域建造前用各自固定的种子重置（湖 1001、潟湖 2002、外海 3003）。回归：`tests/ecology_test.py` 检查潟湖、外海建造前都重置了种子。
- 本次重置后潟湖、外海的布置与之前不同（一次性），以后改一个水域不会再影响另外两个。

### P-017 鲸群可能游进浅水甚至游上岸，被抬到地面以上　【中｜已修复】
- **现象**：2026-09-25 06:05（新加坡时间）`test:ecology` 报外海西南座头鲸出现在 (-205.3, 16.1, 172.4)——陆地上方 16 m。
- **复现**：把各鲸群领头的航点设在岛中央陆地上推进 20 秒，旧版虎鲸游进水深 8.9 m 的浅水（低于该群最小水深的一半）。
- **原因**：`src/17d_eco_ocean.js` 的 `EcoPod` 只在挑选领头航点时检查水深；跟随者按编队位置追赶、以及两航点之间的直线都可能跨过海岸，最后一行 `y = max(y, 地面 + 2.5)` 又把游上岸的鲸抬到地面以上。
- **修复**：每一步前进前检查前方水深（不足该群最小水深的 60% 或不在外海）就原地转向，领头的立即换航点。回归：`tests/ecology_test.py` 把航点设在陆地上推进 20 秒，所有个体始终在深水里。

## 视觉缺陷

### V-001 别墅通往湖岸的汀步：一块埋进土坡，一块悬在坡面上　【中｜已修复】
- **位置**：(171.5, -64) 埋入地面 0.28 m；(171.42, -67.75) 顶面高出旁边地面 0.41 m、悬在坡上。
- **来源**：场景体检"埋地"与"可行走面未登记"；截图核实（巡检 01 别墅西北泳池、问题特写 31、03）。
- **原因**：`src/06_struct_a.js` 里汀步按固定高度 `y0 - 0.1` 摆放，没有跟随湖岸坡面。
- **修复**：每块汀步按四边中点地面平均取高，并沿坡向倾斜贴合坡面，同时登记可行走面。对比图：`docs/visual_fixes/汀步.jpg`。

### V-002 厨房吊柜悬在半空，离墙约 0.9 m　【中｜已修复】
- **位置**：别墅首层厨房 (184.4, -40.25)，底部高 13.55，6×0.8×0.38 m。
- **原因**：吊柜与台面对齐放在局部 z=5.75，而南墙在 z=7，中间没有墙可挂。
- **来源**：场景体检"悬空"。
- **修复**：操作台背后加一道到顶的厨房背墙（从入户门洞东侧 x=2.3 起，不挡门），吊柜挂在墙上，并登记墙线段。对比图：`docs/visual_fixes/厨房吊柜.jpg`。

### V-003 住宅楼入口的花坛和长椅离地　【低｜已修复】
- **位置**：花坛 (±10, -135) 离地 0.21～0.45 m；长椅 (±5.5, -134.5) 离地 0.71～1.81 m。
- **来源**：场景体检"悬空"（该处地面有坡度，外侧是道路边坡，起伏 1.5 m）。
- **修复**：绿篱与长椅移到楼前平整地带（z≈-138.6 / -138.0），按所在地面最低点取高，碰撞随之移动。对比图：`docs/visual_fixes/住宅楼入口.jpg`。

### V-004 栈桥桩柱、水闸墩没有触及海底　【低｜已修复】
- **位置**：栈桥桩 (3.5, 80.5)、(3.5, 93.5)，底部离海底约 4.4 m；水闸墩 (0, 128)，离海底 4.25 m。潜水时能看见。
- **来源**：场景体检"悬空"。
- **修复**：系缆钢桩与水闸中央墩向下延伸到海底以下 0.3 m。门槛未动（开闸时门叶会穿过它，延伸会造成新的穿插）。对比图：`docs/visual_fixes/栈桥桩柱水下.jpg`、`水闸墩水下.jpg`。

### V-005 鸡舍窗与别墅南门花池悬空　【低｜已修复】
- 鸡舍三扇窗 (-99/-97/-95, 48.03) 在木墙顶以上、悬在空中；别墅南门花池离地 0.15～0.17 m。
- **修复**：窗移到木墙上；花池按最低地面落座。对比图：`docs/visual_fixes/鸡舍窗.jpg`、`别墅南门花池.jpg`。

### V-006 共面闪烁　【低｜大部分已修复】
- 场景体检找到 153 处重合面积 ≥ 0.05 m² 的共面，按面积排前几位：
  - 机库地坪与机库主体底面 (-211, -135)，重合 4 m²；
  - 牛棚楼板底与柱端 (-201, -58)，14 处；
  - 水闸塔基座与闸墙 (41.75, 118～120)；
  - 住宅楼每层窗框与墙面 (±4.35/±11.85, -140)。
- 静态截图看不出闪烁，需要在移动视角下核实。完整清单见 `reports/audit/report.md`。
- **修复**：
  - 住宅楼等处 `framedGlass()` 竖梃背面与玻璃背面共面，竖梃前移 5 mm（126 处）；
  - 别墅三层檐口顶面与木地板共面（4.68 m²），檐口降 5 mm；
  - 沙发扶手与底座侧面共面，扶手外扩 5 mm。
- **检测器改进**：朝下且贴地的面看不见，不再计入（机库地坪、牛棚柱端等）。
- **剩余**：16 处小面积（≤ 0.35 m²），分布在塔楼窗框与柱、水闸塔台阶、住宅楼阳台等处，暂未处理。
- 2026-09-25 01:40（新加坡时间）：东闸墩码头台阶改从警戒塔塔基顶面起步后，“水闸塔台阶”3 处（42, 118～122）不再共面，已从 `tests/known_issues.json` 移除。

### V-007 车辆越过坡顶时车底陷入地面　【低｜已修复】
- **位置**：别墅进场路 x 13～76、z -22～-42 一带，车身中心比地面低 0.5～0.7 m（Cybertruck 车轮半径 0.445）。
- **原因**：`src/13_drive.js` 的 `placeCar` 用四个车轮处的地面高度平均值定车身高度，凸起的坡顶处中心就偏低了。
- **来源**：巡逻"车底入地"。实际多出现在越过水田田埂时。
- **修复**：车身高度不低于"车身中线地面最高点减 0.75 倍车轮半径"。驾驶基线断言全部通过。对比图：`docs/visual_fixes/车越坡顶.jpg`。

### V-008 植被整株没入地面　【低｜已修复】
- 13 处灌木/小树整株在地面以下（如 (-29.5, -167.2)、(-57.5, -159.1)）。看不见，但白白占用三角面。
- **原因与修复**：`src/08_vegetation.js` 中伴生灌木在主植株旁随机偏移后仍沿用主植株的地面高度，陡坡上一批悬空、一批埋地，改为按偏移后位置取地面高度（随机数调用顺序不变）。另外梯田挡墙脚下的稻丛会整丛埋进墙里，已跳过这些位置。

### V-009 直升机尾部比例待复核　【低｜已修复】（原 HANDOVER 待办）
- **现象**：尾梁显得偏长。实测模型（米）与 H125 真实数据对比：

| 项目 | 修改前 | 真实 H125 |
|---|---|---|
| 机身长（机头到尾鳍后缘） | 10.40 | 10.93 |
| 旋翼转动时全长 | 13.00 | 12.94 |
| 旋翼轴到机头 / 到机尾 | 2.80 / 7.60 | ≈3.33 / 7.60 |
| 座舱宽 | 1.72 | 1.87 |
| 高度（到桨毂顶） | 3.59 | 3.34 |
| 主旋翼直径 | 10.80 | 10.69 |
| 尾桨位置 | 尾梁左侧 | 尾梁右侧 |

- **原因**：尾梁本身长度正确，是前舱短了 0.53 m、座舱窄了 0.15 m，显得尾部偏长；另外整机偏高 0.25 m，尾桨装反了一侧（`src/07_vehicles.js` 的 `buildH125`）。
- **修复**：按真实比例重新定位。前舱以旋翼轴为基点沿机身方向拉伸 1.19 倍（风挡、滑橇前段、驾驶舱内饰随之移动），截面宽放大到 1.87 m，旋翼降到桨毂顶 3.34 m（整流罩降 0.1 m，保持与桨叶的间隙），桨叶半径 5.345 m，尾桨移到右侧；驾驶座视点前移。修改后实测：机身长 10.93、全长 12.95、高 3.34、主旋翼直径 10.70、座舱宽 1.87。对比图：`docs/visual_fixes/直升机右侧.jpg`、`直升机左侧.jpg`、`直升机尾部斜后.jpg`。
- **数据来源**：Airbus H125 / AS350 B3 公开规格（机身长 10.93 m、全长 12.94 m、主旋翼 10.69 m、尾桨 1.86 m、座舱宽 1.87 m、尾桨在尾梁右侧）。高度采用空客给出的 3.34 m（另有来源为 3.14 m，对应低滑橇）。

### V-010 游艇后甲板朝船尾方向的地面材质偏灰　【低｜已修复】（原 HANDOVER 待办）
- **现象**：游泳平台上连接登岸浮台的舷梯是灰色金属（`M.alu`），在一片柚木和白色船体中显得发灰。
- **修复**：按你的决定改为白色铝合金。新增材质 `M.aluWhite`（白色烤漆铝合金），只用于舷梯，没有改动共享的 `M.alu`。对比图：`docs/visual_fixes/游艇船尾正后.jpg`、`游艇船尾俯视.jpg`。

### V-014 游艇尾封板中部有 V 形缺口，能透过船尾看到船体内部　【低｜已修复】
- **位置**：游艇船尾，后甲板与游泳平台之间（船体局部 x≈-24.9）。
- **来源**：核对 V-010 时截图发现。
- **原因**：船体尾封板是从 (x=-LOA/2, y=1.2) 向尾部截面各点连成的扇形，但截面是开口的（从一舷甲板边经龙骨到另一舷甲板边），两舷甲板边之间那一段没有三角形，留下一个顶点朝下的 V 形缺口。
- **修复**：补上连接两舷甲板边的那个三角形，尾封板变成完整的白色船体面。对比图：`docs/visual_fixes/游艇船尾正后.jpg`。

### V-013 东峰塔顶控制台挡在楼梯出口　【低｜已修复】
- **位置**：观察室 (219.7, -147.6)，高 77.9。控制台正好压在最后一跑楼梯的出口上，人会穿过它；直接加碰撞又会堵死楼梯（`phys_test` 的东峰塔登顶用例会失败）。
- **修复**：控制台移到观察室西墙内侧并登记碰撞，东峰塔登顶用例（77.93）通过。对比图：`docs/visual_fixes/塔顶控制台.jpg`。

### V-011 水闸底槛完全在水下　【无需处理】
- 场景体检"误沉水下"，(0, 128.4)。这是水工结构，设计如此。

### V-012 作物"悬空 0.41 m"　【无需处理：检测误报，检测器已修正】
- 场景体检报了 97 处，截图核实（问题特写 06、16、25）番茄、蔬菜都立在垄上。垄面高度不在地形数据里，检测器看不到。检测器已改为：一个实例化网格中八成以上实例离地的整批视为刻意设计（垄上作物、挂在植株上的果实），只报告少数离群实例。

## 审美建议（只记录，不自动改）

### A-001 沙滩近看偏灰褐　【已解决：根因是程序缺陷 P-013，另按你的要求做了银白细沙与冲岸浪】
- 巡检"沙滩"机位：人眼高度看过去，湿沙带和干沙都偏灰褐，和"纯白沙滩"的设定有差距。

### A-002 住宅楼后方切坡是大片光滑暗面
- 巡检"住宅楼"机位：平台后面的人工切坡是一整片深色斜面，可以加岩面纹理、挡土墙或攀援植物。

### A-003 瞭望塔、西端塔四周植被过密
- 近景机位被树冠完全遮挡，塔身几乎看不到。可以在塔基周围留出一圈空地。
